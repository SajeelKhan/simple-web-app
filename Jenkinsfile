pipeline {
    agent any

    tools{
        nodejs 'node-16'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building Stage'
                //sh 'npm install'
            }
        }
        
    }
}
